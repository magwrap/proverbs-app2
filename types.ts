type InitialDarkThemeState = {
  isDarkTheme: boolean;
};

type InitialScoreState = {
  score: number;
};

type WordType = {
  text: string;
  // len: number;
  start?: number;
  end?: number;
};
