type InitialDarkThemeState = {
  isDarkTheme: boolean;
};

type InitialScoreState = {
  score: number;
};

type WordType = {
  id: number;
  text: string;
  // len: number;
  start?: number;
  end?: number;
};
